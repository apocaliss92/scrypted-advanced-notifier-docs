---
title: MQTT
description: MQTT broker setup for Home Assistant autodiscovery
---

# MQTT Transport

The MQTT transport publishes entity state to an MQTT broker. Home Assistant discovers entities automatically via MQTT device-based autodiscovery.

For the list of discovered entities and shared settings, see [Discovery](/docs/advanced-notifier/discovery).

## Step 1: Enable MQTT

Go to **General → General** and enable **MQTT enabled**.

![Enable MQTT in General tab](/assets/MQTT/MQTT%20-%20enable.png)

## Step 2: Configure Credentials

Go to **General → MQTT**:

- **If the MQTT plugin is already configured in Scrypted** — check **Use MQTT plugin credentials** to reuse those credentials.

![Use MQTT plugin credentials](/assets/MQTT/MQTT%20-%20auto%20credentials.png)

- **Otherwise** — uncheck **Use MQTT plugin credentials** and set the broker URL, username, and password manually.

![Manual MQTT credentials](/assets/MQTT/MQTT%20-%20manual%20credentials.png)

## MQTT-specific Settings

| Setting | Description |
|---------|-------------|
| **Cache topics in memory** | Caches MQTT topics in memory to reduce broker load. Recommended for large installations or cloud MQTT brokers |

## Step 3: Set Discovery Layer

Go to **General → Homeassistant** and set **Discovery layer** to **mqtt** (this is the default).

## Related

- [Discovery](/docs/advanced-notifier/discovery) — Shared entity settings and full entity reference
- [Home Assistant](/docs/advanced-notifier/homeassistant) — HA connection, credentials, and automations
