---
title: Stored Images
description: Filesystem storage for detection images
---

# Stored Images

The plugin can store images on the filesystem for every basic detection and rule.

## Configuration

In the plugin page under **Storage** tab:

- **Storage path** — If set, images used for MQTT topics are also stored on the drive
